import WidgetKit
import SwiftUI

struct MediumSizeView: View {
    
    var entry: SimpleEntry

    var body: some View {
        ZStack {
            Background()
            VStack {
                Text("ok")
            }
        }
    }
}
